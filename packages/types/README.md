# Shared Types

TypeScript type definitions shared between frontend and backend.

## Usage

These types should mirror the Pydantic models in `apps/api/api/models.py` to ensure consistency across the stack.
